{
  "name": "tasksync-pro",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start:client": "cd client && npm run dev",
    "start:server": "cd server && npm run dev",
    "dev": "concurrently -n CLIENT,SERVER -c blue,green \"npm run start:client\" \"npm run start:server\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
